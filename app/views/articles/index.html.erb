<section class="section">
  <div class="container">
    <div class="column">
    	<% if user_signed_in?%>
    		<%= render 'profile'%>
    	<% else %>
      		<%= render 'trends'%>

     	<% end %>
     		<%= render 'feed' %>
      		<%= render 'follow-the-writer' %>
    </div>
  </div>
</section>




